# 코딩테스트 학습 폴더 운영 가이드

이 폴더는 문제 풀이, 채점 기록, 약점 분석, 다음 문제 추천을 위한 개인 학습 공간이다.

## 기본 원칙

- 사용자가 풀이를 완료했다고 알리거나 풀이 파일의 채점을 요청하면, 해당 풀이를 검토한다.
- 문제 원문, 제약 조건, 입출력 예시가 없으면 파일명·주석·사용자가 제공한 정보로 판단한다. 정확한 채점이 불가능할 때만 필요한 정보를 짧게 요청한다.
- 채점은 문제 요구사항 충족 여부, 컴파일·실행 가능성, 시간·공간 복잡도, 경계 조건을 함께 확인한다.
- 문제별 결과는 반드시 `PROGRESS.md`에 한 줄 추가하거나 기존 행을 갱신한다. 통과는 `PASS`, 실패는 `FAIL`로 기록한다.
- 실패한 풀이도 삭제하지 않는다. 실패 원인과 재시도할 핵심을 기록해 학습 자료로 남긴다.
- 기존 사용자의 풀이와 기록은 요청 없이 수정하거나 삭제하지 않는다.

## 풀이 파일 권장 구조

- 문제별로 파일명에 플랫폼·문제 번호·짧은 제목을 드러낸다. 예: `leetcode_1_two_sum.py`
- 파일 상단 주석에는 가능한 한 문제 링크, 사용 언어, 알고리즘/자료구조, 시간·공간 복잡도를 적는다.
- 실행 가능한 풀이에는 대표 예시와 경계 조건을 확인할 수 있는 간단한 테스트를 둔다.

## 채점 절차

1. 문제의 요구사항과 제약 조건을 확인한다.
2. 풀이의 알고리즘, 복잡도, 예외·경계 처리를 검토하고 가능한 경우 실행한다.
3. 결과를 아래 형식으로 간결하게 전달한다.
   - `PASS`: 핵심 접근, 복잡도, 잘한 점, 더 나은 대안이 있으면 한 줄 제안
   - `FAIL`: 재현 가능한 반례 또는 오류, 원인, 최소 수정 방향
4. `PROGRESS.md`의 문제 이력을 갱신한다.
5. 누적 기록을 바탕으로 약점과 다음 학습 문제를 제안한다.

## 기록 규칙

`PROGRESS.md`의 한 행은 한 문제를 뜻한다.

- `날짜`: 채점하거나 풀이를 기록한 날짜 (`YYYY-MM-DD`)
- `플랫폼/번호`: 예: `LeetCode #1`
- `문제`: 문제 제목
- `카테고리`: 아래 표준 카테고리를 쉼표로 구분해 기록
- `난이도`: `Easy`, `Medium`, `Hard` 또는 플랫폼 난이도
- `결과`: `PASS` 또는 `FAIL`
- `언어`: 풀이 언어
- `핵심 접근`: 사용한 알고리즘 또는 자료구조
- `실패 원인 / 배운 점`: 실패 원인, 반례, 또는 통과 후 배운 점
- `재도전`: 실패하면 재도전 권장일 또는 `재풀이 필요`, 통과하면 `-`

## 표준 카테고리

`Array`, `String`, `Hash Map`, `Two Pointers`, `Sliding Window`, `Stack`, `Queue`, `Linked List`, `Binary Search`, `Sorting`, `Prefix Sum`, `Heap/Priority Queue`, `Tree`, `Trie`, `Graph`, `BFS`, `DFS`, `Union Find`, `Backtracking`, `Dynamic Programming`, `Greedy`, `Interval`, `Bit Manipulation`, `Math`, `Matrix`, `Simulation`, `Design`

새 카테고리가 필요하면 기존 명칭과 겹치지 않도록 추가하고, 이후 기록에서도 같은 표기를 유지한다.

## 약점 분석 및 문제 추천

- 최근 기록과 전체 기록을 함께 보되, 특히 `FAIL` 횟수·재풀이 결과·특정 카테고리의 반복 실수를 본다.
- 약점 피드백에는 카테고리, 관찰 근거, 보완할 개념·습관을 명확히 쓴다.
- 추천은 한 번에 2~3문제만 제안하며, 약점 카테고리에서 쉬운 복습 → 동급 응용 → 한 단계 높은 문제 순으로 구성한다.
- 추천할 때는 문제명, 난이도, 추천 이유, 연습할 포인트를 함께 적는다. 최신 목록이나 특정 플랫폼의 현황이 필요하면 확인한 뒤 추천한다.
- 재풀이에서 통과한 문제는 기존 실패 기록을 보존하고, 새 행으로 `PASS`를 추가해 개선 추이를 남긴다.
